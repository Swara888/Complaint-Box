This folder contains a basic Naive Bayes model used to suggest
complaint categories.

- Model is trained offline using scikit-learn
- Backend only loads the saved model and vectorizer
- This is a proof-of-concept, not a production ML pipeline
